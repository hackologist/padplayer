# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code personal settings (machine-specific)
.claude/settings.local.json

# Source pad audio: huge raw masters live locally only. The web player
# serves the converted .m4a files (committed); the .wav/.asd never go to git
# (GitHub rejects files >100MB).
*.wav
*.wav.asd
